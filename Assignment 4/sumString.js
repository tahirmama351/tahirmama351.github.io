"use strict";

/**
 * @param{string} the input must be string.
 * @returns{string} the return expexted to be string
 */
function sumString() {
     let str = "Hello";
     str.test = 5;
     alert(str.test);
}
// we can't add string like object, but we can concatinate











